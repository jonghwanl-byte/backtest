name: Run TAA Backtest

on:
  workflow_dispatch: # 수동 실행 버튼을 만듭니다.

jobs:
  run-backtest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # 원하는 파이썬 버전

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy yfinance matplotlib

      - name: Run Backtest Script
        run: python backtest.py

      - name: Upload chart image
        uses: actions/upload-artifact@v4
        with:
          name: backtest-chart      # 다운로드될 압축파일 이름
          path: backtest_result.png # 파이썬이 저장한 파일 이름

